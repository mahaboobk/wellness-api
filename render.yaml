databases:
  - name: mysite
    databaseName: wellness_database_0dn7
    user: postgres
    plan: free

services:
  - type: redis
    name: cache
    ipAllowList: [] # only allow internal connections
    plan: free # optional (defaults to starter)
    maxmemoryPolicy: allkeys-lfu # optional (defaults to allkeys-lru). Rails recommends allkeys-lfu as a default.
  - type: web
    name: mysite
    runtime: ruby
    plan: free
    buildCommand: "./bin/render-build.sh"
    # preDeployCommand: "bundle exec rails db:migrate" # preDeployCommand only available on paid instance types
    startCommand: "bundle exec rails server"
    envVars:
      - key: wellnessdatabase.cluster-c3k886y44ign.ap-south-1.rds.amazonaws.com
        fromDatabase:
          name: mysite
          property: connectionString
      - key: a1156177f6ab6ebb7cb762bb8138d79f
        sync:false- key: WEB_CONCURRENCY
        value: 2 # sensible default
      - key: REDIS_URL # this must match the name of the environment variable used in production.rb
        fromService:
          type: redis
          name: cache
          property: connectionString